# VITE_API_BASE_URL: Base URL of the Python FastAPI backend (see /backend).
# The frontend calls this directly for /health, /api/options, /api/predict,
# /api/what-if, /api/dataset-info, /api/model-metrics.
VITE_API_BASE_URL="http://localhost:8000"
